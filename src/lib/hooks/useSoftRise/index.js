import softRiseStyle from './useSoftRise';
import makeStyles from '@material-ui/core/styles/makeStyles';

const useSoftRise = makeStyles(softRiseStyle, { name: 'SoftRiseShadow' });

export { softRiseStyle, useSoftRise };

export default useSoftRise;
